<span class="badge">
  <slot />
</span>

<style>
  .badge {
    font-size: 0.6rem;
    border-radius: 7px;
    padding: 0.2rem 0.5rem;
    background-color: var(--accent);
    color: var(--accent-foreground);
  }
</style>
